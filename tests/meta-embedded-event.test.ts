import {describe,expect,it} from "vitest";import {parseMetaEmbeddedEvent} from "@/lib/meta-embedded-event";
describe("eventos oficiales Embedded Signup",()=>{it("captura FINISH y metadatos",()=>expect(parseMetaEmbeddedEvent("https://www.facebook.com",JSON.stringify({type:"WA_EMBEDDED_SIGNUP",event:"FINISH",data:{business_id:"1",waba_id:"2",phone_number_id:"3",verified_name:"Empresa"}}))).toMatchObject({kind:"finish",assets:{business_id:"1",verified_name:"Empresa"}}));it("distingue cancelación y error",()=>{expect(parseMetaEmbeddedEvent("https://web.facebook.com",{type:"WA_EMBEDDED_SIGNUP",event:"CANCEL"})).toEqual({kind:"cancel"});expect(parseMetaEmbeddedEvent("https://facebook.net",{type:"WA_EMBEDDED_SIGNUP",event:"ERROR"})).toEqual({kind:"error"})});it("rechaza origen, JSON o evento incompleto",()=>{expect(parseMetaEmbeddedEvent("https://evil.example",{})).toBeNull();expect(parseMetaEmbeddedEvent("null",{})).toBeNull();expect(parseMetaEmbeddedEvent("https://facebook.com","{")).toBeNull();expect(parseMetaEmbeddedEvent("https://facebook.com",{type:"OTHER"})).toBeNull();expect(parseMetaEmbeddedEvent("https://facebook.com",{type:"WA_EMBEDDED_SIGNUP",event:"FINISH",data:{waba_id:"2"}})).toBeNull()})});