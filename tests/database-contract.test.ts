import {describe,it,expect} from "vitest"; import fs from "node:fs"; import path from "node:path"; const sql=fs.readFileSync(path.join(process.cwd(),"supabase/migrations/202607290001_initial_multitenant.sql"),"utf8"); const whatsappSql=fs.readFileSync(path.join(process.cwd(),"supabase/migrations/202607290002_whatsapp_test_events.sql"),"utf8");
describe("arquitectura multi-tenant",()=>{it("incluye todas las tablas requeridas",()=>{for(const table of ["profiles","organizations","organization_members","contacts","conversations","messages","pipelines","pipeline_stages","deals","tasks","integrations","integration_accounts","webhook_events","audit_logs","data_deletion_requests"])expect(sql).toContain(`create table public.${table}`)});it("crea organización y propietario en una función transaccional",()=>expect(sql).toContain("create_organization_with_owner"));it("habilita RLS y políticas por membresía",()=>{expect(sql).toContain("enable row level security");expect(sql).toContain("is_org_member(organization_id)")});it("impone idempotencia de eventos",()=>expect(sql).toContain("event_key text not null unique"));it("agrega estados e índices de WhatsApp",()=>{expect(whatsappSql).toContain("delivered_at");expect(whatsappSql).toContain("messages_external_id_unique")});});
