import {describe,it,expect} from "vitest"; import {createHmac} from "node:crypto"; import {parseSignedRequest,verifyMetaSignature} from "@/lib/meta-security";
describe("seguridad Meta",()=>{it("valida firma del webhook",()=>{const body='{"id":"event-1"}',secret="test-secret";const sig="sha256="+createHmac("sha256",secret).update(body).digest("hex");expect(verifyMetaSignature(body,sig,secret)).toBe(true)});it("rechaza firma incorrecta",()=>expect(verifyMetaSignature("{}","sha256=00","secret")).toBe(false));it("valida signed_request",()=>{const secret="secret",payload=Buffer.from(JSON.stringify({user_id:"1"})).toString("base64url");const sig=createHmac("sha256",secret).update(payload).digest("base64url");expect(parseSignedRequest(`${sig}.${payload}`,secret)).toMatchObject({user_id:"1"})});});
