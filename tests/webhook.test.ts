import {describe,it,expect} from "vitest"; import {GET,POST} from "@/app/api/meta/webhook/route";
describe("webhook GET",()=>{it("devuelve challenge con token correcto",async()=>{process.env.META_VERIFY_TOKEN="safe-test-token";const r=GET(new Request("https://example.com/api/meta/webhook?hub.mode=subscribe&hub.verify_token=safe-test-token&hub.challenge=123"));expect(r.status).toBe(200);expect(await r.text()).toBe("123")});it("rechaza token incorrecto",()=>{process.env.META_VERIFY_TOKEN="safe-test-token";const r=GET(new Request("https://example.com/api/meta/webhook?hub.mode=subscribe&hub.verify_token=wrong&hub.challenge=123"));expect(r.status).toBe(403)});});
describe("webhook POST",()=>{it("confirma sin registrar datos cuando la integracion aun esta deshabilitada",async()=>{delete process.env.META_APP_SECRET;delete process.env.SUPABASE_SERVICE_ROLE_KEY;const r=await POST(new Request("https://example.com/api/meta/webhook",{method:"POST",headers:{"content-type":"application/json"},body:"{\"object\":\"whatsapp_business_account\",\"entry\":[]}"}));expect(r.status).toBe(200);expect(await r.json()).toEqual({status:"received",processing:"disabled"})})});
