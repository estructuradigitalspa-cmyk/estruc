export type EmbeddedAssets={business_id:string;waba_id:string;phone_number_id:string;display_phone_number?:string;verified_name?:string;business_name?:string;currency?:string};
export type EmbeddedEvent={kind:"finish";assets:EmbeddedAssets}|{kind:"cancel"}|{kind:"error"};
export function parseMetaEmbeddedEvent(origin:string,input:unknown):EmbeddedEvent|null{let hostname="";try{hostname=new URL(origin).hostname}catch{return null}if(!/(^|\.)facebook\.(com|net)$/.test(hostname))return null;let payload=input;try{if(typeof payload==="string")payload=JSON.parse(payload)}catch{return null}const value=payload as {type?:string;event?:string;data?:Record<string,string>};if(value.type!=="WA_EMBEDDED_SIGNUP")return null;if(value.event==="CANCEL")return{kind:"cancel"};if(value.event==="ERROR")return{kind:"error"};const data=value.data;if(value.event!=="FINISH"||!data?.business_id||!data.waba_id||!data.phone_number_id)return null;return{kind:"finish",assets:{business_id:data.business_id,waba_id:data.waba_id,phone_number_id:data.phone_number_id,display_phone_number:data.display_phone_number,verified_name:data.verified_name,business_name:data.business_name,currency:data.currency}}}