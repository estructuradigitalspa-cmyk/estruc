import type { LucideIcon } from "lucide-react"; export function ServiceGrid({services,limit}:{services:{title:string;description:string;icon:LucideIcon}[];limit?:number}){return <div className="cards">{services.slice(0,limit).map(({title,description,icon:Icon})=><article className="service-card" key={title}><span className="icon-wrap"><Icon size={22} aria-hidden="true"/></span><h3>{title}</h3><p>{description}</p></article>)}</div>}
