"use client";import {useState} from "react";
export function PublicQuotationDecision({token}:{token:string}){const[message,setMessage]=useState("");async function decide(decision:"accepted"|"rejected"){const r=await fetch(`/api/public/quotations/${token}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({decision})});const b=await r.json();setMessage(r.ok?(decision==="accepted"?"Cotización aceptada.":"Cotización rechazada."):b.error)}return <div className="button-row"><button className="button button-primary" onClick={()=>decide("accepted")}>Aceptar</button><button className="button button-secondary" onClick={()=>decide("rejected")}>Rechazar</button>{message&&<p>{message}</p>}</div>}
