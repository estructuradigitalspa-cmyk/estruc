"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export function AppointmentForm({requests,members}:{requests:{id:string;description:string|null}[];members:{user_id:string;profiles:{full_name:string|null}|null}[]}){const router=useRouter();const[requestId,setRequestId]=useState(requests[0]?.id??"");const[assignee,setAssignee]=useState("");const[start,setStart]=useState("");const[end,setEnd]=useState("");const[message,setMessage]=useState("");async function book(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/appointments",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"book",serviceRequestId:requestId,assigneeId:assignee||null,startsAt:new Date(start).toISOString(),endsAt:new Date(end).toISOString(),capacityUnits:1,maxCapacity:1,address:null,commune:null})});const b=await r.json();setMessage(r.ok?"Reserva confirmada.":b.error);router.refresh()}return <form className="data-panel form-grid" onSubmit={book}><label className="field"><span>Solicitud</span><select value={requestId} onChange={e=>setRequestId(e.target.value)}>{requests.map(x=><option value={x.id} key={x.id}>{x.description||x.id}</option>)}</select></label><label className="field"><span>Responsable</span><select value={assignee} onChange={e=>setAssignee(e.target.value)}><option value="">Sin asignar</option>{members.map(x=><option value={x.user_id} key={x.user_id}>{x.profiles?.full_name||x.user_id}</option>)}</select></label><label className="field"><span>Inicio</span><input required type="datetime-local" value={start} onChange={e=>setStart(e.target.value)}/></label><label className="field"><span>Fin</span><input required type="datetime-local" value={end} onChange={e=>setEnd(e.target.value)}/></label>{message&&<p className="app-alert">{message}</p>}<button className="button button-primary">Reservar</button></form>}
export function CancelAppointment({id}:{id:string}){const router=useRouter();async function cancel(){await fetch("/api/appointments",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"cancel",appointmentId:id,reason:"Cancelada por operador"})});router.refresh()}return <button onClick={cancel}>Cancelar</button>}
