"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export function QuotationActions({id}:{id:string}){const router=useRouter();const[message,setMessage]=useState("");async function action(value:string){const r=await fetch(`/api/quotations/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({action:value})});const b=await r.json();setMessage(r.ok?(b.simulated?"Envío simulado; no se contactó a nadie.":"Acción registrada."):b.error);router.refresh()}return <div><div className="button-row">{["approve","ready_to_send","simulate_send","expire","cancel"].map(x=><button className="button button-secondary" key={x} onClick={()=>action(x)}>{x}</button>)}</div>{message&&<p className="app-alert">{message}</p>}</div>}
