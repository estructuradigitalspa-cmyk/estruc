"use client"; import Link from "next/link"; import { Menu, X } from "lucide-react"; import { useState } from "react"; import { siteConfig } from "@/lib/site-config"; import { Wordmark } from "./wordmark";
export function Header(){const[open,setOpen]=useState(false);const links=<>{siteConfig.navigation.map(i=><Link onClick={()=>setOpen(false)} href={i.href} key={i.href}>{i.label}</Link>)}<Link onClick={()=>setOpen(false)} href="/iniciar-sesion">Iniciar sesión</Link><Link className="nav-contact" onClick={()=>setOpen(false)} href="/registro">Probar Elytsa</Link></>;return <header className="site-header"><div className="container nav-shell"><Wordmark/><nav className="desktop-nav" aria-label="Navegación principal">{links}</nav><button className="mobile-toggle" aria-expanded={open} aria-controls="mobile-menu" aria-label={open?"Cerrar menú":"Abrir menú"} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div><nav id="mobile-menu" className={`mobile-menu ${open?"open":""}`} aria-label="Navegación móvil">{links}</nav></header>}
