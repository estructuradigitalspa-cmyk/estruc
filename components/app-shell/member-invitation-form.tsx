"use client";
import {useState} from "react";
export function MemberInvitationForm({role}:{role:string}){const[status,setStatus]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setStatus("Enviando…");const form=event.currentTarget,data=new FormData(form);const response=await fetch("/api/organization/invitations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:data.get("email"),role:data.get("role")})});const body=await response.json() as {status?:string;error?:string};setStatus(body.error||(response.ok?"Invitación enviada.":"Error"));if(response.ok)form.reset()}return <section className="integration-detail"><h2>Invitar miembro</h2><form onSubmit={submit} className="auth-form"><label>Correo<input name="email" type="email" required maxLength={254}/></label><label>Rol<select name="role" defaultValue="viewer"><option value="viewer">Viewer</option><option value="agent">Agent</option>{role==="owner"&&<option value="admin">Admin</option>}</select></label><button className="button button-primary" type="submit">Enviar invitación</button></form>{status&&<p className="form-status" aria-live="polite">{status}</p>}</section>}