"use client"; import Link from "next/link"; import { usePathname } from "next/navigation"; import { Blocks, Bot, Cable, CheckSquare, ChevronLeft, ChevronRight, Contact, LayoutDashboard, MessagesSquare, Settings, Users } from "lucide-react"; import { useState } from "react";
const nav=[["/app/inicio","Inicio",LayoutDashboard],["/app/conversaciones","Conversaciones",MessagesSquare],["/app/contactos","Contactos",Contact],["/app/crm","CRM",Blocks],["/app/tareas","Tareas",CheckSquare],["/app/automatizaciones","Automatizaciones",Bot],["/app/integraciones","Integraciones",Cable],["/app/equipo","Equipo",Users],["/app/configuracion","Configuración",Settings]] as const;
export function AppShell({organization,userEmail,children}:{organization:string;userEmail:string;children:React.ReactNode}){const path=usePathname();const[open,setOpen]=useState(false);return <div className="app-frame"><aside className={`app-sidebar ${open?"open":""}`}><div className="app-logo"><span className="wordmark-mark"><i/><i/><i/><i/></span><strong>Estructura</strong><button onClick={()=>setOpen(false)} aria-label="Cerrar navegación"><ChevronLeft/></button></div><div className="org-chip"><span>Organización</span><strong>{organization}</strong></div><nav>{nav.map(([href,label,Icon])=><Link className={path===href?"active":""} href={href} key={href}><Icon size={18}/>{label}</Link>)}</nav><div className="app-user"><span>{userEmail}</span><form action="/auth/signout" method="post"><button>Cerrar sesión</button></form></div></aside><div className="app-main"><header className="app-topbar"><button onClick={()=>setOpen(true)} aria-label="Abrir navegación"><ChevronRight/></button><Link href="/">Volver al sitio</Link></header>{children}</div></div>}

