import type { Metadata } from "next"; import { Mail, MapPin, Phone } from "lucide-react"; import { PageHero } from "@/components/page-hero"; import { ContactForm } from "@/components/contact-form"; import { siteConfig } from "@/lib/site-config";
export const metadata:Metadata={title:"Contacto",description:"Conversa con Estructura Digital sobre software, automatización, CRM e integraciones.",alternates:{canonical:"/contacto"}};
export default function Page(){return <><PageHero eyebrow="Contacto" title="Cuéntanos qué proceso necesitas ordenar o conectar." description="Describe el contexto y el objetivo. Revisaremos la solicitud para responder con próximos pasos claros."/><section className="section"><div className="container contact-grid"><div><h2>Hablemos</h2><p>Atendemos proyectos y consultas comerciales desde Chile.</p><div className="contact-list"><a href={`mailto:${siteConfig.email}`}><Mail size={19}/>{siteConfig.email}</a><a href={`tel:${siteConfig.phoneHref}`}><Phone size={19}/>{siteConfig.phone}</a><span><MapPin size={19}/>{siteConfig.country}</span></div><p className="notice">No publicamos una dirección exacta mientras no esté confirmada. Los datos comerciales se mantendrán actualizados en esta página.</p></div><ContactForm/></div></section></>}
