"use server"; import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { organizationSchema } from "@/lib/validation/schemas";
export async function createOrganization(formData:FormData){const parsed=organizationSchema.safeParse(Object.fromEntries(formData));if(!parsed.success)redirect("/onboarding?error=Revisa los datos de la empresa.");const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/iniciar-sesion");const{error}=await supabase.rpc("create_organization_with_owner",{organization_name:parsed.data.name,organization_country:parsed.data.country,organization_website:parsed.data.website||null,organization_size:parsed.data.size,organization_objective:parsed.data.objective});if(error)redirect("/onboarding?error=No fue posible crear la organización.");redirect("/app/inicio")}
