import {FlaskConical} from "lucide-react";import {requireAppContext} from "@/lib/supabase/app-context";import {ServiceSimulator} from "@/components/service-simulator";
export default async function Page(){const c=await requireAppContext();const{data}=await c.supabase.from("service_agents").select("id,name").eq("organization_id",c.organization.id).eq("active",true);return <main className="app-content"><div className="app-heading"><span className="app-icon"><FlaskConical/></span><div><p>Laboratorio seguro</p><h1>Simulador</h1><span>Persistente, auditable y sin WhatsApp, correo ni contactos productivos.</span></div></div><ServiceSimulator agents={data??[]}/></main>}
