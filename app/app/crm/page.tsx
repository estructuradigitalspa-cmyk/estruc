import { Blocks } from "lucide-react"; import { AppPage } from "@/components/app-shell/app-page"; export default function Page(){return <AppPage eyebrow="Gestión comercial" title="CRM" description="Pipelines, etapas y oportunidades de la organización." icon={Blocks} emptyTitle="Aún no hay oportunidades." emptyDescription="Crea un pipeline cuando el proceso comercial esté definido."/>}
