import {notFound} from "next/navigation";import {Wrench} from "lucide-react";import {requireAppContext} from "@/lib/supabase/app-context";import {ServiceEditor} from "@/components/service-editor";
export default async function Page({params}:{params:Promise<{id:string}>}){const c=await requireAppContext();const{id}=await params;const{data}=await c.supabase.from("service_catalog").select("*,service_variants(*),service_extras(*),service_pricing_rules(*)").eq("organization_id",c.organization.id).eq("id",id).maybeSingle();if(!data)notFound();return <main className="app-content"><div className="app-heading"><span className="app-icon"><Wrench/></span><div><p>Catálogo</p><h1>{data.name}</h1><span>Editor de oferta, requisitos y cálculo.</span></div></div><ServiceEditor service={data}/></main>}
