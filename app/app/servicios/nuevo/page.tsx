import {Wrench} from "lucide-react";import {requireAppContext} from "@/lib/supabase/app-context";import {ServiceEditor} from "@/components/service-editor";
export default async function Page(){const c=await requireAppContext();if(!["owner","admin"].includes(c.membership.role))return <main className="app-content"><p className="app-alert">No tienes permisos para crear servicios.</p></main>;return <main className="app-content"><div className="app-heading"><span className="app-icon"><Wrench/></span><div><p>Catálogo</p><h1>Nuevo servicio</h1><span>Configura el servicio, sus requisitos y precios acotados.</span></div></div><ServiceEditor/></main>}
