import { Contact } from "lucide-react"; import { AppPage } from "@/components/app-shell/app-page"; export default function Page(){return <AppPage eyebrow="Clientes" title="Contactos" description="Personas asociadas a conversaciones y procesos." icon={Contact} emptyTitle="Aún no hay contactos." emptyDescription="Los contactos se crearán manualmente o desde integraciones autorizadas."/>}
