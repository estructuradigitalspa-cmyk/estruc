import { Bot } from "lucide-react"; import { AppPage } from "@/components/app-shell/app-page"; export default function Page(){return <AppPage eyebrow="Flujos" title="Automatizaciones" description="Reglas y acciones supervisadas por la organización." icon={Bot} emptyTitle="Aún no hay automatizaciones." emptyDescription="Este módulo se habilitará gradualmente según los procesos definidos."/>}
