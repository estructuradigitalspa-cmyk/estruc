import Link from "next/link";import {Rocket} from "lucide-react";import {requireAppContext} from "@/lib/supabase/app-context";import {PilotSettingsForm} from "@/components/pilot-settings-form";import {PilotChecklist} from "@/components/pilot-checklist";
const defaults={llm_provider:"simulated",llm_model:"simulated-service-agent-v1",llm_enabled:false,monthly_token_budget:100000,monthly_cost_budget:10,per_run_token_limit:4000};
export default async function Page(){const c=await requireAppContext();const[s,i]=await Promise.all([c.supabase.from("organization_pilot_settings").select("*").eq("organization_id",c.organization.id).maybeSingle(),c.supabase.from("pilot_checklist_items").select("item_key,completed").eq("organization_id",c.organization.id).order("item_key")]);const settings={...defaults,...s.data};return <main className="app-content"><div className="app-heading"><span className="app-icon"><Rocket/></span><div><p>P1 · staging seguro</p><h1>Centro del piloto</h1><span>Configura IA, presupuesto y recorre el piloto sin activar mensajes reales.</span></div></div><div className="pilot-actions"><Link href="/app/configuracion/operacion">1. Empresa</Link><Link href="/app/servicios/nuevo">2. Servicio</Link><Link href="/app/agentes/nuevo">3. Empleado IA</Link><Link href="/app/simulador">4. Conversar</Link><Link href="/app/cotizaciones/nueva">5. Cotizar</Link><Link href="/app/agenda">6. Reservar</Link><Link href="/app/metricas">7. Métricas</Link></div>{["owner","admin"].includes(c.membership.role)&&<PilotSettingsForm initial={settings} credentialReady={Boolean(process.env.LLM_API_KEY)} environment={process.env.APP_ENV||"local"}/>}<PilotChecklist items={i.data??[]}/></main>}
