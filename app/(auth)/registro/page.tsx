import Link from "next/link"; import { AuthCard } from "@/components/app-shell/auth-card"; import { signUp } from "../actions";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <AuthCard title="Crea tu cuenta" description="La organización se configura después de confirmar tu correo." footer={<span>¿Ya tienes cuenta? <Link href="/iniciar-sesion">Inicia sesión</Link></span>}><>{p.error&&<p className="form-status error">{p.error}</p>}<form action={signUp} className="auth-form"><label>Nombre<input name="name" required minLength={2} autoComplete="name"/></label><label>Correo<input name="email" type="email" required autoComplete="email"/></label><label>Contraseña<input name="password" type="password" required minLength={8} autoComplete="new-password"/></label><button className="button button-primary">Crear cuenta</button></form></></AuthCard>}

