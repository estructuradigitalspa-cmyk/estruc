import {AuthCard} from "@/components/app-shell/auth-card";import {updatePassword} from "../actions";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <AuthCard title="Actualiza tu contraseña" description="Define una contraseña nueva para recuperar tu acceso." footer={null}><>{p.error&&<p className="form-status error">{p.error}</p>}<form action={updatePassword} className="auth-form"><label>Nueva contraseña<input name="password" type="password" minLength={12} maxLength={128} required autoComplete="new-password"/></label><button className="button button-primary">Guardar contraseña</button></form></></AuthCard>}