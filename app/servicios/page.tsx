import type { Metadata } from "next"; import { PageHero } from "@/components/page-hero"; import { services } from "@/content/site-content";
export const metadata:Metadata={title:"Servicios",description:"Desarrollo de software, automatización, CRM, inteligencia artificial e integraciones para empresas.",alternates:{canonical:"/servicios"}};
export default function Page(){return <><PageHero eyebrow="Servicios" title="Construimos tecnología alrededor de tu operación." description="Desde sistemas a medida hasta integraciones entre plataformas, cada solución parte de una necesidad concreta y un alcance verificable."/><section className="section"><div className="container content-grid">{services.map(({title,description,icon:Icon,details})=><article className="detail-card" key={title}><span className="icon-wrap"><Icon size={22}/></span><h2>{title}</h2><p>{description}</p><ul>{details.map(i=><li key={i}>{i}</li>)}</ul></article>)}</div></section><section className="section section-soft"><div className="container prose"><h2>Inteligencia artificial con supervisión</h2><p>La inteligencia artificial puede apoyar tareas de clasificación, análisis y generación de respuestas. No sustituye obligatoriamente la supervisión humana: cada implementación considera el contexto, el nivel de riesgo y los controles apropiados.</p></div></section></>}
