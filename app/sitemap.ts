import type { MetadataRoute } from "next"; import { siteConfig } from "@/lib/site-config"; export default function sitemap():MetadataRoute.Sitemap{const paths=["","/servicios","/soluciones","/plataforma","/integraciones","/nosotros","/contacto","/privacidad","/terminos","/eliminacion-de-datos","/seguridad","/proveedor-tecnologia"];return paths.map(path=>({url:`${siteConfig.url}${path}`,lastModified:new Date(),changeFrequency:path===""?"weekly":"monthly",priority:path===""?1:.7}))}

